<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
        Tema claro principal de TaskMaster.
        Hereda de Material3 DayNight para que los atributos
        colorSurface, colorOnSurface, colorOnSurfaceVariant, etc.
        estén disponibles en los layouts.
    -->
    <style name="Theme.TaskMaster" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Colores primarios -->
        <item name="colorPrimary">@color/tm_primary</item>
        <item name="colorPrimaryVariant">@color/tm_primary_dark</item>
        <item name="colorSecondary">@color/tm_accent</item>

        <!-- Fondo de la ventana -->
        <item name="android:windowBackground">@color/surface_light</item>

        <!-- StatusBar -->
        <item name="android:statusBarColor">@color/tm_primary_dark</item>
        <item name="android:navigationBarColor">@color/surface_light</item>

        <!-- Shapes redondeadas -->
        <item name="shapeAppearanceSmallComponent">@style/ShapeSmall</item>
        <item name="shapeAppearanceMediumComponent">@style/ShapeMedium</item>
        <item name="shapeAppearanceLargeComponent">@style/ShapeLarge</item>
    </style>

    <!--
        Tema oscuro de TaskMaster.
        Se aplica desde MainActivity cuando el usuario activa el modo oscuro.
    -->
    <style name="Theme.TaskMaster.Dark" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/tm_primary_light</item>
        <item name="colorPrimaryVariant">@color/tm_primary</item>
        <item name="colorSecondary">@color/tm_accent</item>
        <item name="android:windowBackground">@color/surface_dark</item>
        <item name="android:statusBarColor">@color/surface_dark</item>
        <item name="android:navigationBarColor">@color/surface_dark</item>
        <item name="shapeAppearanceSmallComponent">@style/ShapeSmall</item>
        <item name="shapeAppearanceMediumComponent">@style/ShapeMedium</item>
        <item name="shapeAppearanceLargeComponent">@style/ShapeLarge</item>
    </style>

    <!-- Shapes redondeadas -->
    <style name="ShapeSmall" parent="ShapeAppearance.Material3.SmallComponent">
        <item name="cornerSize">8dp</item>
    </style>
    <style name="ShapeMedium" parent="ShapeAppearance.Material3.MediumComponent">
        <item name="cornerSize">12dp</item>
    </style>
    <style name="ShapeLarge" parent="ShapeAppearance.Material3.LargeComponent">
        <item name="cornerSize">16dp</item>
    </style>

    <!-- Tema para la splash screen / launcher -->
    <style name="Theme.TaskMaster.Launcher" parent="Theme.TaskMaster">
        <item name="android:windowBackground">@color/tm_primary</item>
    </style>

</resources>
